
const initialState = {
    mode: 'night',
}

const reducer = (state = initialState, action:any) => {
    const { payload } = action
    switch (action.type) {
    default: {
        return state
    }
  }
}

export default reducer